groups:
  # - name: "Container Images"
  #   description: "Image to be used for container"
  # - name: "OneDev Configuration"
  #   description: "Configuration details for OneDev workload"
  - name: "Storage"
    description: "Configure Storage for OneDev"
  # - name: "Container Configuration"
  #   description: "Configure OneDev container parameters"
  - name: "Postgresql Configuration"
    description: "Configure Postgresql for OneDev"
  - name: "Scaling/Upgrade Policy"
    description: "Configure how pods are replaced when configuration is upgraded"
  # - name: "Advanced DNS Settings"
  #   description: "Configure DNS settings"

# portals:
#   web_portal:
#     protocols:
#       - "$kubernetes-resource_configmap_nginx-configuration_protocol"
#     host:
#       - "$variable-nextcloud.host"
#     ports:
#       - "$variable-service.nodePort"

questions:
  # Update strategy
  - variable: updateStrategy
    description: "Upgrade Policy"
    label: "Update Strategy"
    group: "Scaling/Upgrade Policy"
    schema:
      type: string
      default: "Recreate"
      enum:
        - value: "RollingUpdate"
          description: "Create new pods and then kill old ones"
        - value: "Recreate"
          description: "Kill existing pods before creating new ones"

  # - variable: service
  #   description: "OneDev Service Configuration"
  #   label: "OneDev Service Configuration"
  #   group: "OneDev Configuration"
  #   schema:
  #     type: dict
  #     required: true
  #     attrs:
  #       - variable: nodePort
  #         label: "Node Port to use for OneDev"
  #         schema:
  #           type: int
  #           min: 9000
  #           max: 65535
  #           default: 9001
  #           required: true

  - variable: appVolumeMounts
    label: "OneDev Storage"
    group: "Storage"
    schema:
      type: dict
      attrs:
        - variable: onedev-data
          label: "OneDev Data Volume"
          schema:
            type: dict
            attrs:
              - variable: datasetName
                label: "OneDev Data Volume Name"
                schema:
                  type: string
                  hidden: true
                  $ref:
                    - "normalize/ixVolume"
                  show_if: [["hostPathEnabled", "=", false]]
                  default: "ix-onedev_data"
                  editable: false
              - variable: mountPath
                label: "OneDev Data Mount Path"
                description: "Path where the volume will be mounted inside the pod"
                schema:
                  type: path
                  hidden: true
                  editable: false
                  default: "/opt/onedev"
              - variable: hostPathEnabled
                label: "Enable Host Path for OneDev Data Volume"
                schema:
                  type: boolean
                  default: false
                  show_subquestions_if: true
                  subquestions:
                    - variable: hostPath
                      label: "Host Path for OneDev Data Volume"
                      schema:
                        type: hostpath
                        required: true

  - variable: postgresAppVolumeMounts
    label: "Postgres Storage"
    group: "Storage"
    schema:
      type: dict
      attrs:
        - variable: postgres-data
          label: "Postgres Data Volume"
          schema:
            type: dict
            attrs:
              - variable: datasetName
                label: "Postgres Data Volume Name"
                schema:
                  type: string
                  hidden: true
                  $ref:
                    - "normalize/ixVolume"
                  show_if: [["hostPathEnabled", "=", false]]
                  default: "ix-postgres_data"
                  editable: false
              - variable: mountPath
                label: "Postgresql Data Mount Path"
                description: "Path where the volume will be mounted inside the pod"
                schema:
                  type: path
                  hidden: true
                  editable: false
                  default: "/var/lib/postgresql/data"
              - variable: hostPathEnabled
                label: "Enable Host Path for OneDev Postgres Data Volume Name"
                schema:
                  type: boolean
                  default: false
                  show_subquestions_if: true
                  subquestions:
                    - variable: hostPath
                      label: "Host Path for OneDev Postgres Data Volume Name"
                      schema:
                        type: hostpath
                        required: true
        - variable: postgres-backup
          label: "Postgres Backup Volume"
          schema:
            type: dict
            attrs:
              - variable: datasetName
                label: "Postgres Backup Volume Name"
                schema:
                  type: string
                  hidden: true
                  $ref:
                    - "normalize/ixVolume"
                  show_if: [["hostPathEnabled", "=", false]]
                  default: "ix-postgres_backups"
                  editable: false
              - variable: mountPath
                label: "Postgresql Backup Mount Path"
                description: "Path where the volume will be mounted inside the pod"
                schema:
                  type: path
                  hidden: true
                  editable: false
                  default: "/postgres_backups"
              - variable: hostPathEnabled
                label: "Enable Host Path for OneDev Postgres Backup Volume Name"
                schema:
                  type: boolean
                  default: false
                  show_subquestions_if: true
                  subquestions:
                    - variable: hostPath
                      label: "Host Path for OneDev Postgres Backup Volume Name"
                      schema:
                        type: hostpath
                        required: true
